import 'package:flutter/material.dart';

class Bingo extends StatelessWidget {
  const Bingo({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Bingo"));
  }
}
